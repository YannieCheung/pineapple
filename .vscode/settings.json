// 将设置放入此文件中以覆盖默认值和用户设置。
{
        //-------- GIT 配置 --------

    // 是否启用了 GIT
    "git.enabled": true,

    // 可执行 GIT 的路径
    "git.path": "https://github.com/YannieCheung/pineapple.git",

    // 是否启用了自动提取。
    "git.autofetch": true
}